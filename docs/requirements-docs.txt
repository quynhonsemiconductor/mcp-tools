# Documentation build dependencies (mkdocs). Pinned exactly so a docs build is
# reproducible and so the dependency scanner can tell which version is in use —
# unpinned entries are reported as 0.0.0 and treated as vulnerable.
#
# markdown must stay above 3.8: every version up to and including 3.8 is affected
# by CVE-2025-69534, where malformed HTML-like input raises an unhandled
# AssertionError during parsing.
#
# pymdown-extensions must stay at 11.0.1 or above, which is the first release
# clear of all three advisories against it: path traversal in the b64 extension
# (fixed 11.0.0), ReDoS in the caret, tilde, betterem and magiclink parsers
# (fixed 11.0.1), and ReDoS in Figure Capture (fixed 10.16.1).

mkdocs==1.6.1
mkdocs-material==9.7.7
pymdown-extensions==11.0.2
markdown==3.10.3
mdx_truly_sane_lists==1.3
mkdocs-gen-files==0.6.1
